{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "yarn dev (stdin: input/data.txt)",
      "runtimeExecutable": "yarn",
      "runtimeArgs": ["dev"],
      "preLaunchTask": "tsc: build - tsconfig.json",

      // ここを追加
      "console": "internalConsole",
      "internalConsoleOptions": "openOnSessionStart"
    }
  ]
}
